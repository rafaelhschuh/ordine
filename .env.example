# ===========================================
# ORDINE - Configuração de Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e ajuste os valores conforme necessário

# -------------------------------------------
# BACKEND - Servidor API
# -------------------------------------------
# Porta onde o servidor backend será executado
PORT=8000

# Host do backend - use 0.0.0.0 para aceitar conexões de qualquer interface de rede
BACKEND_HOST=0.0.0.0

# Lista de origens permitidas para CORS
# Use * para permitir qualquer origem (útil em desenvolvimento)
# Em produção, liste as URLs específicas separadas por vírgula
# Exemplo: ALLOWED_ORIGINS=http://192.168.1.100:8001,http://192.168.1.100:8002
ALLOWED_ORIGINS=*

# -------------------------------------------
# FRONTEND - Configuração dos Apps Vue.js
# -------------------------------------------
# URL base da API para os apps frontend
# IMPORTANTE: Para acesso de outros dispositivos na rede, substitua 'localhost' pelo IP da sua máquina
# Como descobrir seu IP:
#   - Linux/Mac: ip addr show | grep "inet " ou ifconfig
#   - Windows: ipconfig
# Exemplo: VITE_API_BASE=http://192.168.1.100:8000
VITE_API_BASE=http://localhost:8000

# URL do servidor WebSocket (geralmente a mesma do backend)
# Para acesso remoto, use o mesmo IP configurado em VITE_API_BASE
VITE_SOCKET_URL=http://localhost:8000

# Host dos servidores Vite - use 0.0.0.0 para aceitar conexões externas
VITE_HOST=0.0.0.0

# -------------------------------------------
# PORTAS DOS PAINÉIS FRONTEND
# -------------------------------------------
# Porta do Painel de Display (TV)
VITE_DISPLAY_PORT=8001

# Porta do Painel de Controle
VITE_CONTROL_PORT=8002

# Porta do Totem de Emissão de Senhas
VITE_ISSUER_PORT=8003

# -------------------------------------------
# ACESSO REMOTO - INSTRUÇÕES
# -------------------------------------------
# 1. Configure BACKEND_HOST=0.0.0.0 e VITE_HOST=0.0.0.0
# 2. Configure ALLOWED_ORIGINS=* (ou liste os IPs específicos)
# 3. Descubra o IP do servidor (ex: 192.168.1.100)
# 4. Configure VITE_API_BASE e VITE_SOCKET_URL com o IP da máquina
# 5. Acesse os painéis de outros dispositivos:
#    - Painel TV: http://192.168.1.100:8001
#    - Painel Controle: http://192.168.1.100:8002
#    - Totem: http://192.168.1.100:8003
#    - API: http://192.168.1.100:8000
